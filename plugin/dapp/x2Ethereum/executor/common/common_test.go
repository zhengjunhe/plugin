package common

import (
	"testing"
)

func TestEthAddress_String(t *testing.T) {
	//moduleAddress := dapp.ExecAddress(types.ModuleName)
	//fmt.Println(moduleAddress)
	s := Float64ToBytes(0.7)
	ff := BytesToFloat64(s)
	println(ff)
}
